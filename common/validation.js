export function checkDataComingFromPlayer(data){
	// first check player data
	// if (!data.player)
	// 	return false;
	// if (!data.player.id)
	// 	return false;
	// if (!data.player.r)
	// 	return false;
	// if (data.player.vx > 1 || data.player.vx < -1)
	// 	return false;
	// if (data.player.vy > 1 || data.player.vy < -1)
	// 	return false;
	//if (data.player.zoom > 100 || data.player.zoom < 1)
	//	return false;
	return true;
}